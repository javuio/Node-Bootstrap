var chai = require('chai')
var chaiHttp = require('chai-http');
var apiTestHelper = require('../../../apiTestHelper.js');
chai.use(chaiHttp);

var apiUrl = '/api/users/admins/updateUserProfile/';

module.exports = {
    name: 'updateUserProfile',
    test: function (app) {
   
        
        
    }
};